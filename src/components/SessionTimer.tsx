import React from 'react'; import {View,Text} from 'react-native'; export default function SessionTimer({storageKey}:{storageKey:string}){ const [sec,setSec]=React.useState(0); React.useEffect(()=>{const t=setInterval(()=>setSec(s=>s+1),1000); return ()=>clearInterval(t)},[]); const m=Math.floor(sec/60), s=sec%60; return <View><Text>Timer: {m}:{s.toString().padStart(2,'0')}</Text></View> }
