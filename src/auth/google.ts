import { GoogleSignin } from '@react-native-google-signin/google-signin'; import { GoogleAuthProvider, signInWithCredential } from 'firebase/auth'; import { auth } from './firebaseAuth'; export async function configureGoogle(webClientId:string){ GoogleSignin.configure({ webClientId }); } export async function signInWithGoogle(){ const { idToken }=await GoogleSignin.signIn(); const cred=GoogleAuthProvider.credential(idToken); return await signInWithCredential(auth(),cred); }
